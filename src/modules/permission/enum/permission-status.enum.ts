/** Mirrors Meta's call_permissions response (docs/INTEGRATION-META.md §5.4). */
export enum PermissionStatus {
    NO_PERMISSION = "no_permission",
    TEMPORARY = "temporary",
    PERMANENT = "permanent",
}
